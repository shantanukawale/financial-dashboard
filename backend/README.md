# Backend (Stage 2)

This folder is reserved for the financial-dashboard API (auth, database, etc.).

It is intentionally minimal on the `chore/monorepo-stage1` branch so **GitHub Pages** continues to deploy only the static app from [`frontend/`](../frontend/).

See the repository root [README](../README.md) for how to run the frontend.
